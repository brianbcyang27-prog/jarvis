const http=require('http');const https=require('https');const TOKEN='8715045984:AAGw1JqL_mHi5t7PBzlQaE6uv8mjmbxd_3Y';const CHAT=8466621162;function notify(msg){const body=JSON.stringify({chat_id:CHAT,text:msg,parse_mode:'Markdown'});const r=https.request({hostname:'api.telegram.org',path:'/bot'+TOKEN+'/sendMessage',method:'POST',headers:{'Content-Type':'application/json','Content-Length':Buffer.byteLength(body)}},()=>{});r.on('error',()=>{});r.write(body);r.end();}let lastStatus='';http.createServer((req,res)=>{const u=new URL(req.url,'http://localhost');const status=u.searchParams.get('status');const temp=u.searchParams.get('temp');if(status&&status!==lastStatus){lastStatus=status;if(status==='hot'){notify('?? *M4 Overheating!*\nTemp: '+temp+'¡C\nOllama paused Ñ cooling down...');}else if(status==='ok'){notify('? *M4 Cooled Down*\nTemp: '+temp+'¡C\nOllama resumed.');}}res.end('ok');}).listen(9999,'0.0.0.0',()=>console.log('Thermal listener on :9999'));